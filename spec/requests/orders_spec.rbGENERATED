# -------------------------------------------------------------------------------------------------------
# 25/7/14 DH: Only need this level of abstraction if you are developing a client app using the Spree API
# -------------------------------------------------------------------------------------------------------
require 'spec_helper'

describe "Orders" do
  describe "GET /orders" do
    it "works! (now write some real specs)" do
      # Run the generator again with the --webrat flag if you want to use webrat methods/matchers
      puts orders_index_path
      get orders_index_path
      expect(response.status).to be(200)
    end
  end
end
